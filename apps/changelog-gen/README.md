# changelog-gen

A TypeScript CLI that reads conventional-commits from a git repository's history and emits a grouped changelog in markdown or plain text format.

## Development

- `npm test` — run the Vitest test suite
- `npm run dev` — run the CLI via tsx (requires `src/cli.ts`)
